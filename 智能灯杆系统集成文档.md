# 智能灯杆系统集成文档

> 基于 ESP32-S3 + AtomS3R + Flask + iot-light-main (RuoYi-Vue) + EMQX + YOLO

---

## 一、系统架构总览

```
┌────────────────── 硬件层 (本地WiFi网络) ──────────────────┐
│                                                          │
│  AtomS3R M12 摄像头       ESP32-S3-DevKitC-1 单杆控制器   │
│  ├─ /stream (MJPEG)       ├─ DHT11  温湿度(GPIO4)        │
│  ├─ POST /capture (照片)   ├─ 光敏模块 光照(GPIO1)        │
│  └─ POST /api/upload/     ├─ INA219 电压电流(GPIO8/9)     │
│                            ├─ 2路继电器 IN1(GPIO6) 控LED   │
│                            └─ LED(经继电器CH1 + 100Ω)      │
└──────────────┬──────────────────┬────────────────────────┘
               │HTTP               │ MQTT (lamp/LP001)
               ▼                   ▼
┌────────────────── 服务器 (192.168.223.134) ─────────────────┐
│                                                             │
│  EMQX :1883 (MQTT Broker) — 接收 ESP32 上报、转发控制指令    │
│      ↕                                                      │
│  iot-light-main (Spring Boot :8080) — RuoYi 后台管理系统    │
│  ├─ IotMqttMessageService: 收 MQTT → 写 iot_sensor_data    │
│  │                              → 更新 iot_pole.led_status │
│  │                              → 检查报警规则 → 写报警记录   │
│  ├─ 7 个 CRUD Controller (灯杆/传感器/设备/报警/拍照/控制)   │
│  └─ Vue 前端 (:80) — 灯杆列表/详情/实时数据/曲线/报警      │
│                                                             │
│  Flask (:5000) — 摄像头服务                                  │
│  ├─ MJPEG 流代理 (/api/stream/mjpeg)                       │
│  ├─ 照片接收/图库 (/api/upload/photo, /api/photos)          │
│  ├─ SSE 事件 (/api/events)                                 │
│  ├─ 拍照触发 (/api/trigger/set, /api/trigger/consume)       │
│  ├─ 组委会 AI 接口代理 (/infer → YOLO :5001)               │
│  └─ AI 检测线程 → 自动触发拍照 → 写 MySQL iot_camera_capture │
│                                                             │
│  YOLO (:5001) — 独立行人识别进程 (D:\\yolo_model)            │
│  MySQL (:3306) — iot-light 库 (共用)                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、硬件接线（ESP32-S3-DevKitC-1）

### 2.1 引脚分配

| 设备 | 设备引脚 | 接开发板 | 板子位置 |
|---|---|---|---|
| **DHT11** | VCC | 3V3 | J1-1 |
| | GND | GND | J1-22 |
| | DATA | **GPIO4** | J1-4 |
| **光敏模块** | VCC | 3V3 | J1-1 |
| | GND | GND | J1-22 |
| | **AO** | **GPIO1** | **J3-4** |
| | DO | **不接** | — |
| **INA219** | VCC | 3V3 | J1-1 |
| | GND | GND | J1-22 |
| | **SDA** | **GPIO8** | **J1-12** |
| | **SCL** | **GPIO9** | **J1-15** |
| | VIN+ | ← 继电器 NO1 | — |
| | VIN- | → 100Ω → LED 阳极 | — |
| **2路继电器** | **DC+** | **5V** | **J1-21** |
| | **DC-** | **GND** | **J1-22** |
| | **IN1** | **GPIO6** | **J1-6** |
| | IN2 | GPIO7 (备用) | J1-7 |
| | **COM1** | **5V** | **J1-21** |
| | **NO1** | → INA219 VIN+ | — |
| **LED 绿光** | 阳极 | ← 100Ω ← INA219 VIN- | — |
| | 阴极 | GND | J1-22 |
| **AtomS3R M12** | 独立板 | WiFi 无线 | 192.168.223.223 |

> **⚠️ ADC 注意事项**：光敏 AO 接 GPIO1 (ADC1)，ADC2 (GPIO11-20) 连 WiFi 时不可用，已避开。
> **⚠️ GPIO35/36/37**：被 Octal SPI PSRAM 占用，不可使用，已避开。

### 2.2 电流回路图解

```
继电器吸合时:
5V(J1-21) → COM1 → NO1 → INA219 VIN+ → [分流电阻] → VIN- → 100Ω
```
→ LED 阳极 → LED 阴极 → GND(J1-22)

---

## 三、ESP32 固件逻辑

### 3.1 运行流程

```
上电 → 初始化传感器(DHT11/INA219/光敏) → 连WiFi → 连MQTT
→ 每5秒循环:
   1. 读光敏 → 环境暗(lightRaw > 3000) → LED开; 亮(lightRaw < 1000) → LED关
   2. 读DHT11 (30.7°C / 75%)
   3. 读INA219 (LED开时 V≈5V I≈18mA; 关时 0)
   4. MQTT上报 lamp/LP001

    同时:
   - MQTT订阅 lamp/LP001/control, 接收远程开/关灯指令
   - HTTP 轮询 Flask /api/trigger/consume → AtomS3R /capture 拍照
```

### 3.2 MQTT JSON 上报格式（topic: `lamp/LP001`）

```json
{
  "lampId": "LP001",
  "temperature": 30.7,
  "humidity": 75.0,
  "illumination": 13.97,
  "voltage": 5.0,
  "current": 0.018,
  "status": "1"
}
```

**字段说明：**

| 字段 | 类型 | 来源 | 说明 |
|---|---|---|---|
| `lampId` | String | 固定值 | 对应 `iot_pole.pole_code`，必须在 DB 存在 |
| `temperature` | number | DHT11 | 温度 ℃ |
| `humidity` | number | DHT11 | 湿度 % |
| `illumination` | number | 光敏 ADC/100 | 光照强度（自定义单位 0~40.95） |
| `voltage` | number | INA219 / 算法 | LED 工作电压 V |
| `current` | number | INA219 / 算法 | LED 工作电流 A |
| `status` | String | LED 状态 | `"0"` 关 / `"1"` 开 |

> DHT11 故障时 `temperature=-1, humidity=-1`
> LED 关时 `voltage=0, current=0`

### 3.3 MQTT 控制指令订阅（topic: `lamp/LP001/control`）

| 消息 | 效果 |
|---|---|
| `"1"` 或 `{"cmd":"on"}` | 开灯（GPIO6 HIGH → 继电器吸合） |
| `"0"` 或 `{"cmd":"off"}` | 关灯（GPIO6 LOW → 继电器释放） |

### 3.4 光控自动开关灯

```
lightRaw > 3000 (黑暗) → LED ON
lightRaw < 1000 (明亮) → LED OFF
1000 ~ 3000 之间 → 保持原状态 (滞回防抖)
```

> 该模块 AO 为反相输出：越暗 raw 越高 (4095=全黑)，越亮 raw 越低。

---

## 四、MQTT 数据流 (EMQX Broker)

### 4.1 MQTT 连接配置

| 组件 | Broker 地址 | 端口 |
|---|---|---|
| EMQX (服务器) | `tcp://0.0.0.0` | 1883 |
| Spring Boot (RuoYi) | `tcp://localhost` | 1883 |
| ESP32 | `tcp://服务器IP` (如 192.168.223.134) | 1883 |

### 4.2 主题与方向

| Topic | 方向 | 说明 |
|---|---|---|
| `lamp/#` | ESP32 → 服务器 | RuoYi 订阅，接收所有灯杆上报数据 |
| `lamp/LP001` | ESP32 → 服务器 | 灯杆 LP001 上报传感器+LED状态（见 3.2） |
| `lamp/LP001/control` | 服务器 → ESP32 | 下发开/关灯指令（见 3.3） |

### 4.3 EMQX 配置要点

使用 EMQX 默认配置（允许匿名连接），或设置用户名密码：

```bash
# application.yml 对应:
spring.mqtt.broker=tcp://localhost:1883
spring.mqtt.client-id=iot-light
spring.mqtt.username=
spring.mqtt.password=
spring.mqtt.topics=lamp/#
spring.mqtt.qos=1
```

---

## 五、REST API 接口

### 5.1 iot-light-main (RuoYi Spring Boot :8080)

所有接口需携带 `Authorization: Bearer <token>`，先登录获取：

```bash
POST /login
{"username":"admin","password":"admin123"}
```

> **注意**：若关闭认证，在 `application.yml` 中设置 `ruoyi.enabled: false`

| 模块 | 方法 | API 路径 | 说明 |
|---|---|---|---|
| **灯杆** | GET | `/IotPole/IotPole/list` | 灯杆列表（支持 poleCode/poleName 筛选） |
| | GET | `/IotPole/IotPole/{poleId}` | 单杆详情 |
| | PUT | `/IotPole/IotPole` | 修改灯杆信息 |
| **传感器数据** | GET | `/IotSensorData/IotSensorData/list` | 传感器数据列表（poleId 筛选） |
| | GET | `/IotSensorData/IotSensorData/{dataId}` | 单条详情 |
| **摄像头抓拍** | GET | `/IotCameraCapture/IotCameraCapture/list` | 抓拍记录列表（poleId/时间范围筛选） |
| | POST | `/IotCameraCapture/IotCameraCapture` | 新增抓拍记录 |
| | GET | `/IotCameraCapture/IotCameraCapture/{captureId}` | 单条详情 |
| **报警规则** | GET | `/IotAlarmRule/IotAlarmRule/list` | 报警规则列表 |
| | POST | `/IotAlarmRule/IotAlarmRule` | 新增规则 |
| | PUT | `/IotAlarmRule/IotAlarmRule` | 修改规则 |
| **报警记录** | GET | `/IotAlarmRecord/IotAlarmRecord/list` | 报警记录列表 |
| | PUT | `/IotAlarmRecord/IotAlarmRecord` | 处理报警 |
| **控制日志** | GET | `/IotControlLog/IotControlLog/list` | 控制记录列表 |
| **设备** | GET | `/IotDevice/IotDevice/list` | 设备管理列表 |

**摄像头新增抓拍请求体：**

```json
{
  "poleId": 100,
  "imageUrl": "/api/photo/photo_1700000000000.jpg",
  "personCount": 2,
  "resultJson": "[{\"class\":0,\"confidence\":0.95,\"bbox\":[100,200,300,400]}]",
  "captureTime": "2026-08-13 10:00:00"
}
```

**传感器数据筛选示例：**

```
GET /IotSensorData/IotSensorData/list?poleId=100&params[beginTime]=2026-08-13 00:00:00&params[endTime]=2026-08-13 23:59:59
```

### 5.2 Flask 服务器 (:5000)

| 方法 | API 路径 | 说明 |
|---|---|---|
| GET | `/` | 摄像头前端页面 |
| GET | `/api/stream/mjpeg` | MJPEG 实时视频流 |
| GET | `/api/stream/latest` | 最新帧 JPEG |
| GET | `/api/events` | SSE 事件（photo、alarm 等） |
| POST | `/api/upload/photo` | 接收 AtomS3R 拍照（JPEG body） |
| POST | `/api/upload` | 同上（兼容） |
| POST | `/api/upload/stream` | 接收 AtomS3R 流帧 |
| GET | `/api/photos` | 图库列表（按时间倒序） |
| GET | `/api/photo/<filename>` | 单张照片 |
| POST | `/api/trigger/set` | 前端触发拍照 `{"trigger":true}` |
| GET | `/api/trigger/consume` | ESP32 轮询拍照指令 |
| POST | `/infer` | 组委会 AI 接口代理（→YOLO:5001） |
| GET | `/api/camera/ip` | 查询 AtomS3R IP |
| POST | `/api/register/camera` | AtomS3R 自动注册 IP |

**SSE 事件类型：**

```
event: photo
data: photo_1700000000000.jpg    # 新拍照文件
```

### 5.3 YOLO 服务 (:5001)

由组委会提供 `D:\yolo_model\run.bat` 启动，HTTP 接口：

```bash
POST http://127.0.0.1:5001/infer
Content-Type: application/json

{"image": "data:image/jpeg;base64,/9j/4AAQ..."}
```

响应示例：

```json
{
  "image": "(带识别框的Base64 JPEG图片)",
  "inference_results": [
    {"class": 0, "confidence": 0.95, "bbox": [100, 200, 300, 400]}
  ]
}
```

---

## 六、数据库（MySQL :3306）

### 6.1 库名

```
iot-light
```

### 6.2 业务表（7张）

| 表 | 说明 | 关键字段 |
|---|---|---|
| `iot_pole` | 灯杆主表 | `pole_id, pole_code(唯一), pole_name, led_status, status` |
| `iot_device` | 设备清单 | `device_id, pole_id, device_type(0温湿/1光照/2电压电流/3LED/4摄像头)` |
| `iot_sensor_data` | 传感器时序数据 | `data_id, pole_id, temperature, humidity, illumination, voltage, current, collect_time` |
| `iot_alarm_rule` | 报警规则 | `rule_id, pole_id, param_type(0-4), min_value, max_value` |
| `iot_alarm_record` | 报警记录 | `record_id, pole_id, param_type, alarm_value, status` |
| `iot_control_log` | 控制日志 | `log_id, pole_id, control_type(0开灯/1关灯/2重启), result` |
| `iot_camera_capture` | 摄像头抓拍 | `capture_id, pole_id, image_url, person_count, result_json, capture_time` |

### 6.3 种子数据（必需）

```sql
-- 灯杆 LP001 (ESP32 对应)
INSERT INTO `iot_pole` VALUES (100, 'LP001', '灯杆01', '测试路段', NULL, NULL, '0', '0', 1, '0', 'admin', NOW(), '', NULL, NULL);

-- 如需使用 "灯杆01" 作为 lampId, 先执行:
INSERT INTO `iot_pole` VALUES (200, '灯杆01', '灯杆01', '测试路段', NULL, NULL, '0', '0', 1, '0', 'admin', NOW(), '', NULL, NULL);
```

> **ESP32 当前使用 LAMP_ID="LP001"，与 `pole_code='LP001'` 匹配。**
> 若要扩展更多灯杆，每杆在 `iot_pole` 插一行，ESP32 的 `LAMP_ID` 与 `TOPIC_REPORT` 同步修改。

### 6.4 完整建表脚本

见 `sql/data.sql`，包含 RuoYi 系统表 + 7 张业务表 + 种子数据。

---

## 七、部署指南

### 7.1 服务器清单

| 组件 | 端口 | 启动命令 / 说明 |
|---|---|---|
| MySQL 8.0 | 3306 | 运行中，库 `iot-light` |
| EMQX | 1883 | MQTT Broker |
| iot-light-main (RuoYi) | 8080 | `java -jar iotlight-admin.jar` |
| Vue 前端 | 80 | nginx 代理或 `npm run dev` |
| Flask | 5000 | `python app.py` |
| YOLO | 5001 | `D:\yolo_model\run.bat` |

### 7.2 启动顺序

```
1. MySQL            → 导入 data.sql
2. EMQX             → emqx start (或 EMQX 控制台启动)
3. iot-light-main   → java -jar iotlight-admin.jar
4. YOLO             → run.bat
5. Flask            → python app.py
6. AtomS3R M12      → 上电
7. ESP32-S3         → 上电（自动连 WiFi + MQTT）
```

### 7.3 EMQX 安装（Windows）

```bash
# 1. 下载: https://www.emqx.io/downloads?osType=Windows
# 2. 解压到 D:\emqx
# 3. 启动:
D:\emqx\bin\emqx start
# 4. 访问控制台: http://localhost:18083  (默认 admin/public)
# 5. 验证连接:
D:\emqx\bin\emqx_ctl subscribers
```

> **⚠️ 重要**：EMQX 必须先于 RuoYi 和 ESP32 启动，否则它们连不上 broker 会持续报错 rc=-2。

### 7.4 异地访问（另一台电脑查看数据）

系统运行在服务器（如 192.168.223.134），另一台电脑（同一局域网）通过浏览器访问：

| 访问内容 | URL |
|---|---|
| RuoYi 管理后台（Vue 前端） | `http://192.168.223.134` (默认 80 端口) |
| 摄像头实时画面 | `http://192.168.223.134:5000` |
| Spring Boot API | `http://192.168.223.134:8080` |
| YOLO API | `http://192.168.223.134:5001/infer` |

> **注意**：Vue 前端端口由 `start-vue/vue.config.js` 中 `devServer.port` 决定（默认 80）。
> ESP32 的 `mqttHost` 和 `flaskHost` 固定为服务器 IP，设备换网络时需重烧。

---

## 八、数据流总结

### 8.1 传感器数据上报

```
ESP32 每5秒 → 读 DHT11/光敏/INA219
            → MQTT publish "lamp/LP001" (JSON)
            → EMQX → Spring Boot IotMqttMessageService
            → 写入 iot_sensor_data 表
            → 更新 iot_pole.led_status
            → 检查 iot_alarm_rule → 越限则写入 iot_alarm_record
```

### 8.2 行人识别 + 自动拍照

```
Flask 检测线程(每2秒) → 截取最新视频帧 → POST YOLO :5001
→ 检测到行人 → 缓存 AI 结果 + 设置 trigger_flag = True
→ ESP32 轮询 /api/trigger/consume → 收到 true
→ ESP32 POST AtomS3R /capture
→ AtomS3R 拍照 → POST /api/upload/photo → Flask 存照片
→ Flask 读缓存 AI 结果 → INSERT iot_camera_capture (含 person_count + result_json)
→ SSE broadcast "photo" → 前端刷新图库
```

### 8.3 LED 远程控制（通过 MQTT）

```
Web/APP 操作 → RuoYi 后端 → MQTT publish "lamp/LP001/control"
→ EMQX → ESP32 mqttCallback → 控制继电器 IN1 → LED 开关
→ ESP32 下次上报携带 status="0"/"1"
```

---

## 九、关键文件路径

| 文件 | 用途 |
|---|---|
| `arduino_stream/esp32_s3_wroom/esp32_s3_wroom.ino` | ESP32 单杆控制器固件 |
| `arduino_stream/atoms3r_camera/atoms3r_camera.ino` | AtomS3R 摄像头固件 |
| `arduino_stream/server/app.py` | Flask 摄像头服务 |
| `arduino_stream/server/templates/index.html` | Flask 前端页 |
| `iot-light-main/iotlight-admin/src/main/resources/application.yml` | RuoYi 配置 (MQTT 等) |
| `iot-light-main/iotlight-admin/src/main/resources/application-druid.yml` | 数据库连接配置 |
| `iot-light-main/sql/data.sql` | 完整建库脚本 |
| `iot-light-main/IoT-Light-Base/src/main/java/com/iot/service/impl/IotMqttMessageServiceImpl.java` | MQTT 消息处理 |

---

## 十、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| MQTT rc=-2 | EMQX 未启动 | 检查 EMQX 运行状态 |
| MQTT 能连但业务不生效 | `lampId` 与 `iot_pole.pole_code` 不匹配 | 检查 DB 是否有对应灯杆 |
| DHT11 读取失败 | 接线松/供电不足 | 重插 DATA→GPIO4, VCC→3V3 |
| INA219 V=262V | 芯片未响应 (SCL断) | 检查 SDA/SCL 接线 |
| INA219 电流=nan | 克隆板校准失败 | 代码已强制 `setCalibration_32V_2A()` |
| LED 不按光照亮灭 | 阈值/模块反向 | 检查 `lightRaw` 串口值，调 `LIGHT_ON_TH/OFF_TH` |
| 异地电脑打不开页面 | 防火墙/端口未开放 | 关闭 Windows 防火墙或加端口规则 |
| YOLO 返回 503 | YOLO 进程未启动 | 运行 `D:\yolo_model\run.bat` |