<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <artifactId>iotlight</artifactId>
    <groupId>iotlight</groupId>
    <version>3.9.2</version>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <packaging>jar</packaging>
  <groupId>com.iot</groupId>
  <artifactId>IoT-Light-Base</artifactId>
  <version>3.9.2</version>

  <description>
    web服务入口
  </description>

  <dependencies>

    <!-- spring-boot-devtools -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-devtools</artifactId>
      <optional>true</optional> <!-- 表示依赖不会传递 -->
    </dependency>

    <!-- spring-doc -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    </dependency>

    <!-- Mysql驱动包 -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
    </dependency>

    <!-- 核心模块-->
    <dependency>
      <groupId>iotlight</groupId>
      <artifactId>iotlight-framework</artifactId>
    </dependency>

    <!-- 系统模块-->
    <dependency>
      <groupId>iotlight</groupId>
      <artifactId>iotlight-system</artifactId>
    </dependency>

    <!-- 通用工具-->
    <dependency>
      <groupId>iotlight</groupId>
      <artifactId>iotlight-common</artifactId>
    </dependency>

    <!-- 定时任务-->
    <dependency>
      <groupId>iotlight</groupId>
      <artifactId>iotlight-quartz</artifactId>
    </dependency>

    <!-- 代码生成-->
    <dependency>
      <groupId>iotlight</groupId>
      <artifactId>iotlight-generator</artifactId>
    </dependency>

    <!-- mqtt依赖-->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-integration</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.integration</groupId>
      <artifactId>spring-integration-mqtt</artifactId>
    </dependency>

    <!--  eclipsey依赖-->
    <dependency>
      <groupId>org.eclipse.paho</groupId>
      <artifactId>org.eclipse.paho.client.mqttv3</artifactId>
      <version>1.2.5</version>
    </dependency>

  </dependencies>

</project>
