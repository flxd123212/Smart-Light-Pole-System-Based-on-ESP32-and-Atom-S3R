# 智能灯杆系统远程访问文档

> 如何让其他电脑（异地/评委机）通过浏览器访问本系统，进行数据查看与远程控制。

---

## 一、系统有哪些 Web 界面

| 界面 | 端口 | 用途 | 需要登录 |
|---|---|---|---|
| **RuoYi 管理后台** | 80 | 灯杆列表/实时数据/曲线/报警/LED 远程控制 | admin/admin123 |
| **摄像头页面 (Flask)** | 5000 | 实时视频流、照片图库、手动拍照 | 无 |
| **Spring Boot API** | 8080 | REST 接口（供前端调用） | Bearer Token |
| **EMQX 控制台** | 18083 | MQTT Broker 监控 | admin/public |

> 所有服务已绑定 `0.0.0.0`（监听所有网卡），局域网内任意设备均可访问，**无需改代码**。

---

## 二、同一局域网访问（竞赛场景：评委机查看）

### 2.1 确认服务器 IP

在**服务器电脑**上打开命令提示符：

```bat
ipconfig
```

找到当前网络的 **IPv4 地址**（例如 `192.168.223.134`）。

### 2.2 评委机浏览器访问

评委机连接**同一个 WiFi / 交换机**后，浏览器直接输入：

| 访问内容 | 地址 |
|---|---|
| 管理后台 | `http://192.168.223.134` |
| 摄像头页面 | `http://192.168.223.134:5000` |
| 测试 API | `http://192.168.223.134:8080` |

> 评委机不需要安装任何软件，Chrome/Edge 即可。

### 2.3 防火墙放行（必须！）

Windows 防火墙默认阻止外部访问，需放行端口。**服务器电脑**以管理员运行 PowerShell：

```powershell
# 放行 80 / 5000 / 8080 / 1883 / 18083 / 3306
New-NetFirewallRule -DisplayName "IoT-Web"  -Direction Inbound -Protocol TCP -LocalPort 80,5000,8080,1883,18083,3306 -Action Allow
```

或简单粗暴（竞赛现场推荐，省事）：

```powershell
# 关闭防火墙（管理员）
Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False
```

> **竞赛提示**：现场时间紧张，直接关闭防火墙最稳妥。演示结束后再恢复。

### 2.4 验证

评委机浏览器打开 `http://192.168.223.134`，能看到登录页即成功。

---

## 三、跨网络访问（非竞赛场景）

### 3.1 方案 A：Tailscale 虚拟局域网（推荐，无需公网 IP）

1. 服务器和远程电脑都安装 [Tailscale](https://tailscale.com/download)，登录同一账号
2. 服务器上运行 `ipconfig`，找到 `100.x.x.x` 开头的 Tailscale IP
3. 远程电脑浏览器访问 `http://100.x.x.x`

### 3.2 方案 B：路由器端口映射（需公网 IP）

```
路由器管理页 → 端口转发/虚拟服务器：
  外部端口 80   → 内部 192.168.223.134:80
  外部端口 5000 → 内部 192.168.223.134:5000
  外部端口 8080 → 内部 192.168.223.134:8080
```

远程访问 `http://公网IP` / `http://公网IP:5000`。

### 3.3 方案 C：内网穿透（花生壳/ngrok）

无需路由器配置，但免费版速度慢，仅适合演示 API。

---

## 四、硬件侧注意（设备换网络时）

| 固件 | 需修改的配置 | 位置 |
|---|---|---|
| ESP32-S3 | `mqttHost` = 服务器 IP（如 192.168.223.134） | `esp32_s3_wroom.ino` 顶部 |
| ESP32-S3 | `flaskHost` = 服务器 IP | 同上 |
| AtomS3R | `flaskHost` = 服务器 IP | `atoms3r_camera.ino` 顶部 |

> WiFi 名/密码改网络时也需同步修改（当前 `flxd/flxd123212`），改完重新烧录。

---

## 五、常见问题排查

| 现象 | 原因 | 解决 |
|---|---|---|
| 评委机打不开页面 | 防火墙未放行 | 执行 2.3 节命令或关防火墙 |
| 能打开登录页但数据不动 | ESP32 连不上 MQTT（rc=-2） | 检查 EMQX 是否在服务器启动 |
| 视频流黑屏 | AtomS3R 不在同一网络 | 确认 AtomS3R 已上电且连同一 WiFi |
| 页面能开但接口 401 | 未登录 | 用 admin/admin123 登录 |
| 服务器 IP 变了 | DHCP 分配变化 | 路由器里给服务器 MAC 绑定固定 IP |

---

## 六、安全提醒

- 公网暴露有风险，非必要不开端口映射
- 管理后台默认账号 `admin/admin123`，正式使用请改密码
- MQTT 允许匿名连接（EMQX 默认），局域网内风险可控